The utils folder contains reusable helper functions.

Utility functions are small pieces of logic that can be used in multiple parts of the application instead of being duplicated.

Purpose: Keep common helper logic organized and reusable.
